import { useState } from "react"
import { useAuthContext } from "./useAuthContext"

const useRegister = () => {
    const [error, setError] = useState(null)
    const [isLoading, seIsLoading] = useState(null)
    
}